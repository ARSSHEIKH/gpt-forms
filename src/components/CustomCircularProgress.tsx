import { CircularProgress } from "@mui/material";
import styled from "@emotion/styled";

export const CustomCircularProgress = styled(CircularProgress)(({ }) => ({
    color: "#FFF", // Use a theme color,
    fontsize: 12
    // Add other custom styles here
}));
